import React from 'react';
import { Text, View } from 'react-native';

import { font } from '../../constants';

export default function TeacherDashboard({}) {
  return (
    <View>
      <Text style={[font.h1]}>Teacher Dashboard</Text>
    </View>
  );
}
