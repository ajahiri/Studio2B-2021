import React from 'react';
import { Text, View } from 'react-native';

import { font } from '../../constants';

export default function StudentDashboard({}) {
  return (
    <View>
      <Text style={[font.h1]}>Student Dashboard</Text>
    </View>
  );
}
