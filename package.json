{
  "name": "studio2b-2021",
  "version": "0.1.0",
  "scripts": {
    "install-deps": "npm install && npm run install-deps:client && npm run install-deps:server",
    "install-deps:client": "cd ./client && npm install",
    "install-deps:server": "cd ./server && npm install",
    "test:client": "cd ./client && npm test",
    "test:server": "cd ./server && npm test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "2.2.1"
  },
  "dependencies": {
    "react-native-upload-aws-s3": "^0.8.1"
  }
}
